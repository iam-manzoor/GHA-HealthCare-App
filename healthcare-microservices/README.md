# Healthcare Microservices Platform

This folder contains three Java microservices for a healthcare startup:

- `user-service` - user registration/login, JWT authentication, role-based access
- `appointment-service` - appointment booking, history, cancellation
- `report-service` - medical report upload/download, AWS S3 integration

Each service is a standalone Spring Boot application with its own Maven build.
